MY STUDY COMPANION 0.15.17 — CORRECTED SAFE RELEASE STATUS

IMPORTANT RETRACTION
- The APK artifact produced by workflow run 31012705994 is withdrawn from installation use.
- It was signed with an isolated CI packaging certificate rather than the permanent Firebase-registered certificate.
- It also substituted the Firebase Hosting address for the real Cloud Run API endpoint and therefore disabled Smart Online based on an invalid endpoint test.
- Do not install either earlier 0.15.17 APK and do not uninstall the currently working app to make either APK install.

WHAT REMAINS VALID
- The isolated Android feature implementation for pinch zoom, pan, inverse tap mapping, reset controls, milestone feedback, automatic palette advancement, and premium paint-by-number content compiled and passed its source/build tests.
- Those feature changes remain staged on top of the accepted cumulative source through 0.15.16.
- This does not make the earlier APK a safe production update; signing identity and connected-release configuration are mandatory parts of a valid Android release.

ROOT CAUSE
- The 0.15.17 packaging workflow used the wrong release path: it allowed a disposable signer and hardcoded https://my-study-companion-abc01.web.app as though it were the backend API.
- The previously working connected path deploys the private service to Google Cloud Run and uses the deploy action's real service URL.
- Google Sign-In and the cloud architecture were not proven broken. The replacement packaging and endpoint validation were wrong.

CORRECTED RELEASE GATE
- Corrective commit: 8d3c955343cc040fe2b67fb9c3bcb8666f2a6b64
- Workflow: My Study Companion 0.15.17 Safe Canonical Connected Release
- The workflow now refuses all disposable or replacement signing certificates.
- It requires and verifies the permanent Firebase certificate:
  SHA-1: 1997d421d177215a44f9651ce53dbaec152fbc49
  SHA-256: 13a3bb08a32ab57c36ea6f885a74b5bd40d70501f93ca9002df7b6cdd0491b2c
- It restores the previously working Workload Identity, Cloud Run deployment, server-secret verification, Firebase Auth, App Check, Firestore, and GPT-5.6 backend path.
- It accepts only the real Cloud Run .run.app service URL after the complete authenticated 0.15.17 health contract passes.
- It publishes APKs only after the final phone and Wear certificates match the permanent Firebase signing identity and the connected backend URL is verified.

CURRENT SAFE BUILD RESULT
- Safe workflow run: 31021386843
- Result: BLOCKED SAFELY BEFORE APK BUILD
- Exact blocker: the canonical keystore secret MSC_SIGNING_KEYSTORE_B64 and related canonical signing inputs are not present in GitHub Actions.
- No phone APK, Wear APK, or fallback-signed APK was produced by the corrected safe workflow.
- The Cloud Run deployment was intentionally not changed after the signing gate failed.

CURRENT INSTALLATION INSTRUCTION
- Keep the currently installed, working app in place.
- Do not uninstall it.
- Do not install the withdrawn 0.15.17 CI APKs.

WHAT IS REQUIRED FOR THE ACTUAL UPDATE
- The existing permanent Android signing keystore and its passwords/alias must be supplied through the protected MSC_SIGNING_* release inputs, or the same source must be signed in the secure environment that holds that key.
- This private key cannot be recreated, substituted, or bypassed without producing a different Android application identity and risking Google Sign-In and update compatibility.
- Once the canonical key is available, the corrected workflow can deploy and verify the real Cloud Run backend, build the connected APK, prove its signing identity, and publish the only approved 0.15.17 update.
