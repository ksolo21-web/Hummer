name: Classify My Study Companion Build (Paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build classification writes are paused for the 0.9.3 artifact-only build."
