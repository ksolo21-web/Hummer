name: My Study Companion Prerelease Verification (Paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Prerelease verification is paused because 0.9.3 uses private Actions artifacts."
